Template.layout.rendered = function() {
   // the side nav configs 
    $('.button-collapse').sideNav({
      closeOnClick: true // Closes side-nav on <a> clicks, useful for Angular/Meteor
    }
  );
     
}